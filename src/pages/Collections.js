import React from "react";
import ProductsList from "../components/ProductsList";

const Collections = () => {
  return (
    <div>
      <ProductsList />
    </div>
  );
};

export default Collections;
